RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php

# Permite cargar archivos html/js/css... solo desde este dominio
Header set Content-Secure-Policy "default-src 'self';"
